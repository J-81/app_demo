import React, { createContext } from 'react';

export const LoginContext = createContext({})

export const TasksLibraryContext = createContext({})

export const BlocksLibraryContext = createContext({})